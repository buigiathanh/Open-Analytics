# --- OAuth sign-in (Supabase Auth only — app data lives in PostgreSQL) ---
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_APP_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_app_publishable_key

# Public URL of this app (must match browser origin for OAuth PKCE cookies)
NEXT_PUBLIC_APP_URL=http://localhost:3001

# App mode: development | production
# In development, ingest accepts localhost hostnames and bot verify probes use http:// for localhost.
APP_ENV=development

# PostgreSQL connection (projects, events, Search Console tokens)
# Run supabase/schema-postgres.sql once on your database.
POSTGRES_URL=postgresql://user:password@localhost:5432/open_analytics

# Optional: override tracker ingest URL in embed snippets (defaults to {NEXT_PUBLIC_APP_URL}/api/events)
# NEXT_PUBLIC_TRACKER_ENDPOINT=http://localhost:3001/api/events

# Optional: override bot middleware snippet URLs (defaults to {NEXT_PUBLIC_APP_URL}/api/bot-visits)
# NEXT_PUBLIC_BOT_TRACKER_ENDPOINT=http://localhost:3001/api/bot-visits
# NEXT_PUBLIC_BOT_MIDDLEWARE_HELPER_URL=http://localhost:3001/bot-middleware.mjs

# OAuth sign-in: Supabase Dashboard → Authentication → URL Configuration
# Redirect URLs: http://localhost:3001/auth/callback (and production /auth/callback)
# Site URL: same origin as NEXT_PUBLIC_APP_URL
#
# Providers (Authentication → Providers):
# - Google: OAuth client; authorized redirect = https://YOUR_APP_PROJECT.supabase.co/auth/v1/callback
# - GitHub: OAuth App (not GitHub App); callback = https://YOUR_APP_PROJECT.supabase.co/auth/v1/callback
#   Additional scopes in Supabase GitHub provider: read:user user:email
#
# Google Search Console (separate OAuth from Supabase sign-in)
# Cloud Console → APIs & Services → Enable "Google Search Console API"
# OAuth client (Web): redirect URI must include:
#   {NEXT_PUBLIC_APP_URL}/api/integrations/google/search-console/callback
GOOGLE_SEARCH_CONSOLE_CLIENT_ID=
GOOGLE_SEARCH_CONSOLE_CLIENT_SECRET=
# Optional: HMAC secret for OAuth state
# GOOGLE_OAUTH_STATE_SECRET=

# Similarweb public API (no API key). Optional: SIMILARWEB_DEBUG=1 for server logs.
# SIMILARWEB_DEBUG=1

# Realtime WebSocket: run via `npm run dev` / `npm start` (custom server.mjs)
# Protocol: docs/realtime-socket.md
